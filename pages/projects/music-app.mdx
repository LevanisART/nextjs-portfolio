import { withRouter } from 'next/router'
import Project from '../../components/layouts/project'

export const meta = {
  published: true,
  publishedAt: '2020-03-28',
  title: 'Music App UI',
  description: 'A little concept for a mobile app that lets users find and listen to their favorite songs. My main goal was to make the platform clean and easy to use.',
  image: '/static/projects/music-app.png',
  category: 'UI/UX Design',
  dribbble: 'https://dribbble.com/shots/10852439-Music-App'
}

export default withRouter(({ children, router }) => (
  <Project path={router.pathname} meta={meta}>
    {children}
  </Project>
))